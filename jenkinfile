pipeline {
    agent any
    stages {
       stage ('Build stage') {
            steps {
                sh 'mvn  clean package'
            }
        }
        stage ('Test stage') {
            steps {
                sh 'mvn test'
            }
        } 
       
    }
}
